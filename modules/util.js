export const log = (message) => console.log(`Foundry VTT | Kingdoms | ${message}`);
export const warn = (message) => console.warn(`Foundry VTT | Kingdoms | ${message}`);
export const error = (message) => console.error(`Foundry VTT | Kingdoms | ${message}`);
